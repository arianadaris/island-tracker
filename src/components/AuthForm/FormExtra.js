import React from 'react';

const FormExtra = () => {
    return (
        <>
            <p className="text-right mt-[-1.25rem] text-blue-500 hover:text-blue-400 hover:underline cursor-pointer">Forgot password?</p>
        </>
    );
};

export default FormExtra;